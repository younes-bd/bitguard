from django.urls import path, include

urlpatterns = [
    # Commerce
    path('ecommerce/', include('apps.ecommerce.api.urls')),
    path('billing/', include('apps.billing.api.urls')),
    path('accounting/', include('apps.accounting.api.urls')),  # ← FIXED: was missing!
    path('sale/', include('apps.sale.api.urls')),

    # Customer & Sales
    path('crm/', include('apps.crm.api.urls')),
    path('contracts/', include('apps.contracts.api.urls')),
    path('helpdesk/', include('apps.helpdesk.api.urls')),

    # Security Platform
    path('security/', include('apps.soc.api.urls')),

    # Operations
    path('hr/', include('apps.hr.api.urls')),
    path('purchase/', include('apps.purchase.api.urls')),
    path('stock/', include('apps.stock.api.urls')),
    path('delivery/', include('apps.delivery.api.urls')),
    path('projects/', include('apps.projects.api.urls')),
    path('maintenance/', include('apps.maintenance.api.urls')),      # Generic Asset Management
    path('approvals/', include('apps.approvals.api.urls')),
    path('documents/', include('apps.documents.api.urls')),
    path('appointments/', include('apps.appointments.api.urls')),
    path('planning/', include('apps.planning.api.urls')),
    path('field-service/', include('apps.field_service.api.urls')),

    # Platform Infrastructure
    path('auth/', include('apps.auth.api.urls')),
    path('core/', include('apps.core.api.urls')),
    path('users/', include('apps.users.api.urls')),
    path('tenants/', include('apps.tenants.api.urls')),
    path('notifications/', include('apps.notifications.api.urls')),

    path('base_setup/', include('apps.base_setup.api.urls')),

    # Intelligence, Reports & Marketing
    path('board/', include('apps.board.api.urls')),
    path('reporting/', include('apps.reporting.api.urls')),
    path('marketing/', include('apps.marketing.api.urls')),
    path('elearning/', include('apps.elearning.api.urls')),
    path('home/', include('apps.website.api.urls')),
    path('portal/', include('apps.portal.api.urls')),

    # Integrations
    path('blog/', include('apps.blog.api.urls')),
    path('ai/', include('integrations.ai_engine.urls')),
    path('automation/', include('integrations.automation.urls')),
    
    # Newly scaffolded APIs
    path('fleet/', include('apps.fleet.api.urls')),
    path('mrp/', include('apps.mrp.api.urls')),
    path('pos/', include('apps.pos.api.urls')),
    path('rental/', include('apps.rental.api.urls')),
    path('discuss/', include('apps.discuss.api.urls')),
    path('mrp_plm/', include('apps.mrp_plm.api.urls')),
    path('quality_control/', include('apps.quality_control.api.urls')),
]

